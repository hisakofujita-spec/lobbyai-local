{
  "version": 2,
  "installCommand": "pip install -r requirements.txt && python3 scripts/04_indexer.py && gzip -f -k db/minutes.db",
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["db/minutes.db.gz", "templates/**"]
      }
    }
  ],
  "routes": [{"src": "/(.*)", "dest": "app.py"}]
}
