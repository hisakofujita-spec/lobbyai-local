services:
  - type: web
    name: lobbyai
    env: python
    region: singapore
    buildCommand: pip install -r requirements.txt && python3 scripts/04_indexer.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
